void merge ( array, lower bond, upper bond)


clock - returns the cpu time program has used, measured in clock ticks
clocks_Per_Sec - tells you how many ticks corresponding to one second, 
so you can convert the value from clock() into seconds.

